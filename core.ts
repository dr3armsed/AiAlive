/**
 * Defines core constants for the DigitalDNA module.
 * Previously, this was an empty module which may have caused issues
 * with certain build tools, leading to unexpected parsing errors.
 */
export const DNA_CORE_VERSION = '2.0.0';